# Database
# Get these values from your NeonDB dashboard: https://console.neon.tech
# For local development, copy this file to .env.local and fill in your actual values

# DATABASE_URL - Used by your application (with connection pooling)
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL="postgresql://username:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# DIRECT_URL - Used by Prisma migrations (direct connection, no pooling)
# Same as DATABASE_URL but typically points to the direct endpoint
DIRECT_URL="postgresql://username:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Note: In production/preview on Vercel, set these environment variables in the Vercel dashboard
# Production: Use your production NeonDB connection string
# Preview: Use a separate NeonDB branch for preview deployments
